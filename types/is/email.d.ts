/**
 * 是否为邮箱
 *
 * @group Function
 * @category Is
 * @param value 要校验的字符串
 * @returns 是否为邮箱
 * @example
 * isEmail('test@example.com') // true
 */
export declare function isEmail(value: string): boolean;
