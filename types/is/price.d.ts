/**
 * 是否为价格
 *
 * @group Function
 * @category Is
 * @param value 要校验的字符串
 * @returns 是否为价格
 * @example
 * isPrice('10.05') // true
 * isPrice('10.055') // false
 */
export declare function isPrice(value: string): boolean;
